apiVersion: v1
kind: Pod
metadata:
  name: Config-pod
  labels:
    project: roboshop
    environ: dev
  
spec:
  containers:
    - name: nginx
      image: nginx
      envFrom:
        - configMapRef:
            name: config-map