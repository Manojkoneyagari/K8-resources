apiVersion: v1
kind: Service 
metadata: 
  name: service 
  labels: 
    project: roboshop
    env: dev

spec:
  type: NodePort
  selector:
    project: roboshop
    component: nginx

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80